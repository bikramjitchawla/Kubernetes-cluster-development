#apiVersion: networking.k8s.io/v1
#kind: Ingress
#metadata:
#  name: keycloak-discovery
#  namespace: oauth       
#  labels:
#    app: keycloak
#spec:
#  ingressClassName: traefik  
#  rules:
#    - host: keycloak.127.0.0.1.nip.io
#      http:
#        paths:
#          - path: /
#            pathType: Prefix
#            backend:
#              service:
#                name: keycloak-discovery
#                port:
#                  number: 8080


apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: keycloak
  namespace: oauth 
spec:
  entryPoints:
    - web        
  routes:
    - match: Host(`keycloak.127.0.0.1.nip.io`)
      kind: Rule
      services:
        - name: keycloak
          port: 8080
